# ──────────────────────────────────────────────────────────────
# JDE Integrity Analyzer — OCI ADK Agent Dependencies
# ──────────────────────────────────────────────────────────────
# OCI SDK + Agent Development Kit
oci[adk]>=2.133.0

# HTTP client for MCP bridge (ADK tools → MCP server)
httpx>=0.27.0

# PDF text extraction
pypdf>=4.0.0

# API server
fastapi>=0.115.0
uvicorn>=0.30.0

# Markdown → HTML conversion (email formatting)
markdown>=3.7

# Environment variable loading
python-dotenv>=1.0.0
